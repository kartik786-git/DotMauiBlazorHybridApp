
@if(Visible)
{
<div class="confirmation-modal">
    <h3>Confirm Deletion</h3>
    <p>Are you sure you want to delete this card?</p>
    <div>
        <button @onclick="DeleteContact">Yes</button>
        <button @onclick="CancelDelete">No</button>
    </div>
</div>
}

@code {
    [Parameter]
    public bool Visible { get; set; }

    [Parameter]
    public EventCallback CancelBlogClicked { get; set; }

    [Parameter]
    public EventCallback DeleteBlogClicked { get; set; }

    private async void CancelDelete()
    {
        await CancelBlogClicked.InvokeAsync(true);
    }
    private async void DeleteContact()
    {
        await DeleteBlogClicked.InvokeAsync();
    }
}
